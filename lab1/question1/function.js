//** Create a function that takes the age in years and returns the age in days.

function Calc(ageYear)
{
  return `The Age in Year ${ageYear} equal in Days = ${ageYear*365.25}`;
}
console.log(Calc(23));
